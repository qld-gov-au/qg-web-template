import { Canvas, Meta, Story } from "@storybook/addon-docs";

import Default from "./templates/Default.html";
import Icon from "./templates/Icon.html";

import { QgContent } from "../../decorators";

<Meta title="Components/Aside" decorators={[QgContent]} />

# Aside
Requires `#QgContent` ancestor.

## Default

<Canvas withSource="open">
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: Default,
        },
      },
    }}
  >
    {() => Default}
  </Story>
</Canvas>

## Icon

<Canvas withSource="open">
  <Story
    name="Icon"
    parameters={{
      docs: {
        source: {
          code: Icon,
        },
      },
    }}
  >
    {() => Icon}
  </Story>
</Canvas>
