import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { QgPrimaryContent } from "../../decorators";

import Default from "./templates/Default.html";

<Meta title="Components/Document Download" decorators={[QgPrimaryContent]} />

# Document Download
Requires `#QgPrimaryContent` ancestor.

## Default

<Canvas withSource="open">
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: Default,
        },
      },
    }}
  >
    {() => Default}
  </Story>
</Canvas>
