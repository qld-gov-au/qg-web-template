import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { QgPrimaryContent } from "../../decorators";

import Default from "./templates/Default.html";
import PrintGuide from "./templates/PrintGuide.html";

<Meta title="Components/Print" decorators={[QgPrimaryContent]} />

# Print
Requires `#QgPrimaryContent` ancestor.

## Default

<Canvas withSource="open">
  <Story
    name="Default"
    parameters={{
      docs: {
        source: {
          code: Default,
        },
      },
    }}
  >
    {() => Default}
  </Story>
</Canvas>

## PrintGuide

<Canvas withSource="open">
  <Story
    name="PrintGuide"
    parameters={{
      docs: {
        source: {
          code: Default,
        },
      },
    }}
  >
    {() => PrintGuide}
  </Story>
</Canvas>
